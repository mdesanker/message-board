extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href="/new") Create Post 
  each message in messages 
    div.message
      h4= message.text
      p= message.user 
      p= message.added
  
